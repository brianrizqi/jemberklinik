<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="himasif.ilkom.unej.ac.id.jemberklinik">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.EditProfile"></activity>
        <activity android:name=".Activities.DokterTambahKuota" />
        <activity android:name=".Activities.DokterSemuaUser" />
        <activity android:name=".Activities.DokterDetailPemesanan" />
        <activity android:name=".Activities.Login" />
        <activity android:name=".Activities.Dokter" />
        <activity android:name=".Activities.Pasien" />
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>